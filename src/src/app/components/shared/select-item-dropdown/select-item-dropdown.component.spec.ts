import { async} from '@angular/core/testing';
import { IgxDropDownComponent } from '@infragistics/igniteui-angular';

import { SelectItemDropdownComponent } from './select-item-dropdown.component';

describe('SelectItemDropdownComponent', () => {
  let component: SelectItemDropdownComponent; 

  beforeEach(async(() => {
   
  }));

  beforeEach(() => { 
    component = new SelectItemDropdownComponent();
  });

/*   it('should create', () => {
   // spyOn(component.igxDropDown, 'updateTimeStamp');
    expect(component).toBeTruthy();
  }); */
});
